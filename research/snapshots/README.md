# Research snapshots

Automated discovery may write candidate metadata outside this directory, but only human-reviewed, versioned snapshots are published here. A snapshot is immutable after publication and records the catalog hash and review decision.

The working catalog for the current release is [`../catalog.json`](../catalog.json).

